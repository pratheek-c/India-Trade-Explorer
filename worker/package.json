{
  "name": "india-trade-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute india-trade-db --file=./migrations/001_init.sql"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "latest"
  }
}
